##############################################################################################################
#
# The parser for this file was whipped up very quickly and i don't vibe code so it might be very picky.
#   - Inline comments are not supported.
#   - There should be no space between the key and the delimiter(=).
#       > valid: KEY=VALUE
#       > invalid: any form of KEY = VALUE where there is empty space
#   - Invalid keys are ignored
#   - Invalid values will crash the game
#   - If this file or any of its' keys are missing, hardcoded values from the executable are loaded
#   - Hotreloading is not supported
#   - Negative values are reinterpreted as their unsigned counterpart
#
##############################################################################################################

# Seed value for random number generator
# Set to 0, or comment out for random seed
# One use case for this would be to compete with a friend by matching your seeds to generate the same game
SEED=0

# Automatically trigger powerup menu when leveling up
# Note: You can press [TAB] to bring up the powerup menu
POWERUP_MENU_STEALS_FOCUS=0

# Allows you freely toggle the powerup menu
UNLIMITED_POWERUPS=0

# Auto click for touchpad users in boring lectures
AUTO_CLICK=0

# Allows player health to go below 0%
DISABLE_HEALTH_CHECK=0

# Framerate limit
# 0 results in 4x the monitor refresh rate
TARGET_FRAMERATE=0
